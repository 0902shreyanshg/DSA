#include<iostream>
#include <bits/stdc++.h>
#include <math.h>
#include <vector>
#include <map>
#include <set>
#include <algorithm>

using namespace std;

int main()
{
    int arr[] = {11, 21, 31, 41};
    
    // Since, you tried to update SYMBOL TABLE, it showed error
    // i.e. arr = arr + 1 is NOT POSSIBLE
    // whereas, ptr = ptr + 1 is POSSIBLE

// ERROR
    // int *ptr = arr++;

    // cout << *ptr << endl;

    return 0;
}